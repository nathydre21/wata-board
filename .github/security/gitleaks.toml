# gitleaks configuration for wata-board.
# Extends the default ruleset with allowlists for test/example content.

[extend]
useDefault = true

# Allowlist of paths known to contain example/test secrets (not real secrets).
[allowlist]
description = "Ignore test fixtures, examples, and obvious placeholders"
paths = [
  '''security-tests/.*''',
  '''backend/src/test/.*''',
  '''backend/tests/.*''',
  '''frontend/src/test/.*''',
  '''frontend/src/__tests__/.*''',
  '''.env.example''',
  '''backend/.env.example''',
  '''backend/api-testing/.*''',
  '''docs/.*''',
]
regexes = [
  '''example-password''',
  '''change-me''',
  '''YOUR_.*_HERE''',
  '''gho_\*+''',
  '''localhost:[0-9]+''',
  '''127\.0\.0\.1:[0-9]+''',
  '''SABER[0-9a-f]+''',
  '''test_password''',
  '''test_user''',
]
